#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running tests before push..."
npm test --silent
status=$?

if [ $status -ne 0 ]; then
  echo "\nPush aborted: tests failed."
  exit $status
fi

echo "Tests passed. Proceeding with push."


